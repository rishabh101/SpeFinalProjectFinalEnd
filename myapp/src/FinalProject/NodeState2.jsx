import React, { useState } from "react";
import noteContext from "./noteContext";
/*
const NoteState2=(props)=>
{
    const s1=[{}]
    const [subservices,setsubservices]=useState(s1);
    const update=(data)=>
    {
        setsubservices(data);
    }
    return(
        <noteContext.Provider value={{subservices,update}}>
            {props.children}
        </noteContext.Provider>
    )
}
export default NoteState2;
*/